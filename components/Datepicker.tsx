import * as React from 'react';
import { Datepicker as UIDatepicker, DatepickerProps } from '@ui-kitten/components';

export const Datepicker = (props: DatepickerProps) => {
    return (
        <UIDatepicker
            {...props}
        />
    );
};


